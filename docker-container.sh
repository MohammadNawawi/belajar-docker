# TODO : Melihat semua docker container di server
docker container ls -a

# TODO : Melihat semua docker container yang aktif
docker container ls

# TODO : Membuat Docker Container
docker container create --name contohredis redis:latest
